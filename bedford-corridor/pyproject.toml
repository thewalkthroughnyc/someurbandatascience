[project]
name = "bedford-corridor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "geopandas",
    "shapely",
    "requests",
    "pyyaml",
]

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
