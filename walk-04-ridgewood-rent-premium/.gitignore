# raw data: never edited, never committed (cache lives here too)
data/raw/*
!data/raw/.gitkeep

__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
.env

# packet QA previews — regenerate with `python src/make_packet.py --preview`
outputs/packet_preview/
